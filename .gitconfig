[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
        name = Ardalan Samimi
        email = ardalan.samimi@bontouch.com
        signingkey = AECC3F5BDB43AF73
[core]
	excludesfile = /Users/ardalansamimi/.dotfiles/.gitignore_global
	editor = /usr/bin/nano
[commit]
	gpgsign = true
[gpg]
	program = /usr/local/bin/gpg2
[credential]
	helper = osxkeychain
[alias]
	pl = pull
	ps = push
	co = checkout
	st = status
	ci = commit
	up = remote update
	unstage = reset HEAD --
	last = log -1 HEAD
	ignore = update-index --assume-unchanged
	unignore = update-index --no-assume-unchanged
[push]
	followTags = true
